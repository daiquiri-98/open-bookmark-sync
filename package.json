{
  "name": "open-bookmark-sync",
  "version": "1.3.0",
  "description": "Synchronize Raindrop.io collections with Chrome/Brave bookmarks",
  "main": "test-worker.js",
  "scripts": {
    "start": "node test-worker.js",
    "build": "node scripts/build.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^2.6.7"
  }
}